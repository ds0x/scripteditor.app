title:      tell application “Script Editor” to activate
description: "AppleScript can't be contained."
author:     ds
baseurl:    "/scripteditor.app"            # ← leave empty for user sites
url:        "https://ds0x.github.io"   # ← update for project sites

theme:      null          # we’ll use our own CSS
markdown:   kramdown
kramdown:
  input: GFM
